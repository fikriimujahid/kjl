# Product catalog URL used by the Home page.
# Defaults to https://kjl.fikri.dev/public-data/catalog.json when omitted.
NEXT_PUBLIC_PRODUCT_URL="https://kjl.fikri.dev/public-data/catalog.json"

# Optional manual override for static export builds.
# npm run export already triggers export mode automatically.
# NEXT_OUTPUT_MODE="export"
