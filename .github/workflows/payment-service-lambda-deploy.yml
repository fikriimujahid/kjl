name: Payment Service Lambda Deploy

on:
  push:
    branches: [dev, prod]
    paths:
      - backend/services/payment-service/**
      - backend/packages/**
      - backend/tsconfig.base.json
      - backend/package.json
      - backend/package-lock.json
      - .github/workflows/payment-service-lambda-deploy.yml

env:
  AWS_REGION: ${{ vars.AWS_REGION }}

jobs:
  deploy-payment-service-lambda:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'prod' && 'prod' || 'dev' }}
    env:
      ZIP_FILE: payment-service-lambda.zip

    permissions:
      id-token: write
      contents: read

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build --workspace payment-service

      - name: Package Lambda
        run: |
          if [ ! -d services/payment-service/build/lambda ]; then
            echo "Expected services/payment-service/build/lambda directory is missing."
            exit 1
          fi

          rm -f "${ZIP_FILE}"
          cd services/payment-service/build/lambda
          zip -r "../../../../${ZIP_FILE}" .

      - name: Upload Lambda artifact
        uses: actions/upload-artifact@v4
        with:
          name: payment-service-lambda
          path: backend/${{ env.ZIP_FILE }}
          if-no-files-found: error

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ github.ref_name == 'prod' && secrets.AWS_PROD_ROLE_ARN || secrets.AWS_DEV_ROLE_ARN }}

      - name: Create Lambda env file
        env:
          DYNAMO_DB_TABLE_NAME: ${{ secrets.PAYMENT_SERVICE_DYNAMO_DB_TABLE_NAME }}
          MIDTRANS_SERVER_KEY: ${{ secrets.PAYMENT_SERVICE_MIDTRANS_SERVER_KEY }}
          MIDTRANS_SNAP_API_URL: ${{ secrets.PAYMENT_SERVICE_MIDTRANS_SNAP_API_URL }}
          APP_BASE_URL: ${{ secrets.PAYMENT_SERVICE_APP_BASE_URL }}
          PRODUCT_SERVICE_INTERNAL_API_BASE_URL: ${{ secrets.PAYMENT_SERVICE_PRODUCT_SERVICE_INTERNAL_API_BASE_URL }}
          PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY: ${{ secrets.PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY }}
          PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY: ${{ secrets.PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY }}
        run: |
          if [ -z "${DYNAMO_DB_TABLE_NAME}" ]; then
            echo "DYNAMO_DB_TABLE_NAME is missing. Set vars.DYNAMO_DB_TABLE_NAME or secrets.DYNAMO_DB_TABLE_NAME."
            exit 1
          fi

          if [ -z "${MIDTRANS_SERVER_KEY}" ]; then
            echo "MIDTRANS_SERVER_KEY is missing. Set vars.MIDTRANS_SERVER_KEY or secrets.MIDTRANS_SERVER_KEY."
            exit 1
          fi

          if [ -z "${PRODUCT_SERVICE_INTERNAL_API_BASE_URL}" ]; then
            echo "PRODUCT_SERVICE_INTERNAL_API_BASE_URL is missing. Set vars.PAYMENT_SERVICE_PRODUCT_SERVICE_INTERNAL_API_BASE_URL or secrets.PAYMENT_SERVICE_PRODUCT_SERVICE_INTERNAL_API_BASE_URL."
            exit 1
          fi

          if [ -z "${PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY}" ]; then
            echo "PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY is missing. Set vars.PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY or secrets.PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY."
            exit 1
          fi
          
          if [ -z "${PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY}" ]; then
            echo "PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY is missing. Set vars.PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY or secrets.PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY."
            exit 1
          fi

          cat > .env.lambda <<EOF
          DYNAMO_DB_TABLE_NAME=${DYNAMO_DB_TABLE_NAME}
          MIDTRANS_SERVER_KEY=${MIDTRANS_SERVER_KEY}
          MIDTRANS_SNAP_API_URL=${MIDTRANS_SNAP_API_URL}
          APP_BASE_URL=${APP_BASE_URL}
          PRODUCT_SERVICE_INTERNAL_API_BASE_URL=${PRODUCT_SERVICE_INTERNAL_API_BASE_URL}
          PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY=${PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY}
          PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY=${PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY}
          EOF

      - name: Update Lambda environment variables
        env:
          LAMBDA_FUNCTION_NAME: ${{ secrets.PAYMENT_SERVICE_LAMBDA_FUNCTION_NAME }}
          DYNAMO_DB_TABLE_NAME: ${{ secrets.PAYMENT_SERVICE_DYNAMO_DB_TABLE_NAME }}
          MIDTRANS_SERVER_KEY: ${{ secrets.PAYMENT_SERVICE_MIDTRANS_SERVER_KEY }}
          MIDTRANS_SNAP_API_URL: ${{ secrets.PAYMENT_SERVICE_MIDTRANS_SNAP_API_URL }}
          APP_BASE_URL: ${{ secrets.PAYMENT_SERVICE_APP_BASE_URL }}
          PRODUCT_SERVICE_INTERNAL_API_BASE_URL: ${{ secrets.PAYMENT_SERVICE_PRODUCT_SERVICE_INTERNAL_API_BASE_URL }}
          PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY: ${{ secrets.PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY }}
          PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY: ${{ secrets.PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY }}
        run: |
          if [ -z "${LAMBDA_FUNCTION_NAME}" ]; then
            echo "PAYMENT_SERVICE_LAMBDA_FUNCTION_NAME secret is missing."
            exit 1
          fi

          CURRENT_ENV_VARS=$(aws lambda get-function-configuration \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --query 'Environment.Variables' \
            --output json)

          UPDATED_ENV_VARS=$(echo "${CURRENT_ENV_VARS}" | jq -c \
            --arg dynamoDbTableName "${DYNAMO_DB_TABLE_NAME}" \
            --arg midtransServerKey "${MIDTRANS_SERVER_KEY}" \
            --arg midtransSnapApiUrl "${MIDTRANS_SNAP_API_URL}" \
            --arg appBaseUrl "${APP_BASE_URL}" \
            --arg productServiceInternalApiBaseUrl "${PRODUCT_SERVICE_INTERNAL_API_BASE_URL}" \
            --arg productServiceInternalServiceApiKey "${PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY}" \
            --arg paymentServiceInternalServiceApiKey "${PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY}" \
            '. + {
              DYNAMO_DB_TABLE_NAME: $dynamoDbTableName,
              MIDTRANS_SERVER_KEY: $midtransServerKey,
              MIDTRANS_SNAP_API_URL: $midtransSnapApiUrl,
              APP_BASE_URL: $appBaseUrl,
              PRODUCT_SERVICE_INTERNAL_API_BASE_URL: $productServiceInternalApiBaseUrl,
              PRODUCT_SERVICE_INTERNAL_SERVICE_API_KEY: $productServiceInternalServiceApiKey,
              PAYMENT_SERVICE_INTERNAL_SERVICE_API_KEY: $paymentServiceInternalServiceApiKey
            }')

          UPDATE_PAYLOAD=$(jq -cn \
            --arg functionName "${LAMBDA_FUNCTION_NAME}" \
            --argjson variables "${UPDATED_ENV_VARS}" \
            '{FunctionName: $functionName, Environment: {Variables: $variables}}')

          aws lambda update-function-configuration \
            --cli-input-json "${UPDATE_PAYLOAD}"

          aws lambda wait function-updated \
            --function-name "${LAMBDA_FUNCTION_NAME}"

      - name: Deploy Lambda code
        env:
          LAMBDA_FUNCTION_NAME: ${{ secrets.PAYMENT_SERVICE_LAMBDA_FUNCTION_NAME }}
        run: |
          if [ -z "${LAMBDA_FUNCTION_NAME}" ]; then
            echo "PAYMENT_SERVICE_LAMBDA_FUNCTION_NAME secret is missing."
            exit 1
          fi

          aws lambda update-function-code \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --zip-file "fileb://${ZIP_FILE}"
